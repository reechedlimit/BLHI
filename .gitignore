genealogy-tool/venv/
genealogy-tool/__pycache__/
genealogy-tool/*.db-shm
genealogy-tool/*.db-wal
genealogy-tool/.run/
website/assets/*.png
website/assets/*.jpg
# Keep the ones actually used if they are small, but let's be careful with memory.
# Actually, better to just push them if they are part of the site.
